<!DOCTYPE html>
<html>
<head>
  <title>History</title>
</head>
<body style="font-family: Arial, sans-serif; background: #f4f4f4; text-align: center; padding: 40px;">
  <h1 style="color: #333;">Calculation History for <%= username %></h1>

  <% if (history.length > 0) { %>
    <ul style="list-style-type: none; padding: 0; max-width: 400px; margin: auto;">
      <% history.forEach(calc => { %>
        <li style="background: white; margin: 8px 0; padding: 10px; border-radius: 6px; box-shadow: 0 0 5px rgba(0,0,0,0.1);">
          <%= calc.expression %> = <strong><%= calc.result %></strong>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p style="color: #777;">No history found for this user.</p>
  <% } %>

  <a href="/?username=<%= username %>" 
    style="display: inline-block; margin-top: 20px; padding: 10px 15px; background: #2196F3; color: white; border-radius: 4px; text-decoration: none;">
    Back to Calculator
  </a>
</body>
</html>
